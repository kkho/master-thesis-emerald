head	1.2;
access;
symbols
	105alpha:1.2
	104alpha:1.2
	103alpha:1.2
	102alpha:1.2
	101alpha:1.2
	100alpha:1.2
	DIKU_0:1.2;
locks; strict;
comment	@ * @;


1.2
date	94.06.13.21.08.56;	author norm;	state Exp;
branches;
next	1.1;

1.1
date	94.06.08.21.49.43;	author norm;	state Exp;
branches;
next	;


desc
@@


1.2
log
@Fixed compiler errors
@
text
@#include "iisc.h"
#include "sisc.h"
#include "iset.h"
#include "slist.h"
#include "ilist.h"
#include "assert.h"

struct identifier_entry {
    char *name;		/* character string key for this entry */
    int  token;		/* token (id or some keyword) */    
    int  lineno;	/* source line number */
};

typedef struct thing {
  ISet first;
  ISet follow;
  int nrules;
  struct rule **rules;
} thing;

typedef struct rule {
  int nsymbols;
  ISet first;
  ISet director;
  struct identifier_entry **symbols;
} rule;
extern thing *currentthing;
extern rule  *currentrule;
typedef struct identifier_entry symbol;
extern symbol *first;
@


1.1
log
@Initial revision
@
text
@a30 3
#if !defined(_STDLIB_H) && !defined(_H_STDLIB)
extern char *malloc();
#endif
@
